{
  "$schema": "../../../../schemas/command.schema.json",
  "name": "1password:secrets",
  "args": "<command> [arguments...]",
  "options": [
    {
      "flags": "--env-file <path>",
      "description": "Project-relative env file with op:// references."
    },
    {
      "flags": "--environment <id>",
      "description": "1Password Environment ID for op run --environment."
    }
  ],
  "summary": "Run a command with 1Password secrets.",
  "help": "Load secrets into one command's environment via op run. Provide --env-file for op:// references in a project env file, --environment for a 1Password Environment ID, or both (Environment variables take precedence).",
  "steps": [
    {
      "call": {
        "function": "requireSetup"
      }
    },
    {
      "call": {
        "function": "requireCommandArguments",
        "args": ["$@"]
      }
    },
    {
      "call": {
        "function": "runSecretsWrapper",
        "args": ["$@"]
      }
    }
  ]
}
