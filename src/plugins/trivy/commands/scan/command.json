{
  "$schema": "../../../../schemas/command.schema.json",
  "name": "trivy:scan",
  "args": "[path]",
  "summary": "Scan a project path for vulnerabilities with Trivy.",
  "help": "Scans the selected project path inside the development container. The default path is the project root. Writes a JSON report to .docker-dev/reports/trivy.json and exits with a non-zero status when Trivy finds vulnerabilities at the configured severity.",
  "output": { "file": "reports/trivy.json" },
  "steps": [
    {
      "call": {
        "function": "scanTrivyPath",
        "args": ["$@"]
      }
    }
  ]
}
