{
  "$schema": "../../../../../../schemas/command.schema.json",
  "name": "trivy:npm",
  "args": "<project-relative-path> [npm-arguments...]",
  "summary": "Run the protected npm wrapper.",
  "help": "Run npm inside the protected development container.",
  "steps": [
    {
      "call": {
        "function": "runNpmWrapper",
        "args": ["$@"]
      }
    }
  ]
}
