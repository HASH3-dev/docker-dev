{
  "$schema": "../../schemas/plugin.schema.json",
  "name": "gitleaks",
  "kind": "secret-scanner",
  "summary": "Detects hardcoded secrets in the git history and working tree with Gitleaks.",
  "configPath": "gitleaks.config.json",
  "output": { "file": "../../reports/gitleaks.json" }
}
